# --- Data Lake : dépendances Python (API + scripts de transformation) ---

# Accès S3 / MinIO
boto3==1.35.49

# Manipulation de données
pandas==2.2.3
polars==1.12.0
pyarrow==18.1.0          # lecture/écriture Parquet (wheel Python 3.13)
numpy==2.1.3

# Zone curated : Elasticsearch
elasticsearch==8.15.1

# Sources de données
# (API Hacker News = simples requêtes HTTP via `requests`, aucune lib dédiée)
datasets==3.1.0          # datasets HuggingFace

# NLP / sentiment
vaderSentiment==3.3.2    # modèle lexical (baseline, rapide)

# Deep Learning : Transformer RoBERTa fine-tuné sur des tweets.
# Installer torch en version CPU (bien plus léger) :
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
transformers==5.13.0

# API Gateway
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic-settings==2.6.1

# Niveau avancé : optimisation /ingest_fast
numba==0.61.0            # wheel Python 3.13

# Utilitaires
python-dotenv==1.0.1
requests==2.32.3
